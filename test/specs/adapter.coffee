describe "Adapter", ->
    describe "@constructor()", ->
      it "should create new instance of rc.Adapter", ->

    describe "#pick()", ->
      it "should pick suitable format", ->
      it "should parse json properly", ->
      it "should parse yaml properly", ->
      it "should parse ini properly", ->
      it "should parse plist properly", ->

    describe "@parse()", ->
      it "should try to parse config", ->

    describe "@stringify()", ->
      it "should write file with appropriate format", ->
